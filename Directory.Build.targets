<Project>

  <PropertyGroup>
    <!-- Force the documentation of all projects -->
    <!-- Part of the target because OutputPath need to be initialized -->
    <DocumentationFile>$(OutputPath)$(ProjectName).xml</DocumentationFile>
  </PropertyGroup>

</Project>
